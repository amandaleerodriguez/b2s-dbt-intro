version: 2
# always specify which version of yml we are using. RN version 2 is the most recent

sources:
  - name: jaffle_shop
    database: demo_db
    schema: raw_jaffle_shop
    tables:
      - name: orders